# 发布检查表

- [ ] `npm run lint` 通过。
- [ ] `npm run build` 通过并生成离线资产清单。
- [ ] `python3 courseware/verify_all.py` 显示 53 个案例和 53 个实验通过。
- [ ] `npm run test:e2e` 桌面与手机测试通过。
- [ ] 修改静态资源时更新 `public/sw.js` 的缓存版本。
- [ ] 验证 manifest 名称、图标、主题色和 standalone 启动。
- [ ] 在线下载离线课程后，切断网络验证首页、课程代码和 CSV。
- [ ] 验证 Python 取消、45 秒超时和刷新输出恢复。
- [ ] 导出 JSON 备份，在干净浏览器上下文恢复并核对进度。
- [ ] 检查资料链接、教育用途声明和数据合成说明。
- [ ] 更新变更日志与维护文档。
